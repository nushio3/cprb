/*
  def xyz()
  ['X', 'Y', 'Z'].each{|dir| yield(dir)}
  end

  def xyzi()
  ctr = 0
  ['X', 'Y', 'Z'].each{|dir| yield(dir,ctr); ctr+=1}
  end  

  puts "*/

#include <iostream>
using namespace std;

const int N = 10; // the size of mesh
/*"                                             

  xyz{|dir| puts "*/ struct #{dir}{}; /*" }

xyzi{|dir,i| puts "*/
int operator+(int x, #{dir} d) {
  return x + 1#{ ' *N'*i };
}
/*" }

  a = "*/
int main () {
  cout << 0#{' +Y()'*4}#{' +X()'*2} << endl;
  return 0;
} /*"


  puts a
*/


